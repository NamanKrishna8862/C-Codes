#include<stdio.h>

int main(){
    int a;
    int b;
    int c;
    int ab; // While naming variable in c there should be no whitespace, no number, no special characters except _, no brackets.
            // Variables are also case sensitive.
    return 0;
}


// Entity whose value cannot be changed is called constants
// Variables are entities whose value can be altered or changed
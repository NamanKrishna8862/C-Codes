#include<stdio.h>

int main(){
    int a;
    scanf("%d", &a); // This takes in the value from user %d indicating it should be integer and &a depicting to store the input in variable a.
    printf("The value of a is %d", a); 
    return 0;
}
#include<stdio.h>

int main(){
    int a = 1; // We can write the code like this instead of creating a variable first and then putting the value in it.
    float b = 1.4; // float is used for decimal numbers
    char c = "Hello World" ; // char is used to store characters in it.
    // %d - integer, %f - float, %c - character
    // \n is used for placing something in next line
    return 0;
}



#include<stdio.h>

int main(){
    int i = 10;
    int j = i; // We can store the value of some variable in another variable
    int a = 10, b = 20, c = 30; // We can create multiple variables in same line

    printf("The value of i is %d and the value of j is %d", i, j);
}
#include<stdio.h>

int main(){
    int a = 5;
    int b = 2;
    int c = a+b;

    printf("The value of a is %d, the value of b is %d, the sum is %d \n", a,b,c);
    printf("The remainder of a divided by b is %d", a%b); // Module operator % is used to calculate remainder and cannot be applied to float

/*
This code will not work
int j = 3^4 as exponents is not a part of cß
*/
}